<style>
  table,
  td {
    border: 1px solid #2e81a5;
  }

  thead,
  tfoot {
    background-color: #2e81a5;
    color: #e2850b;
  }
</style>
<table>
    <thead>
        <tr>
            <th colspan="5"><%= @cat.name %></th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>id:<%= @cat.id %> </td>
            <td>birthdate:<%= @cat.birth_date %></td>
          	<td>color:<%= @cat.color %></td>
          	<td>name:<%= @cat.name %></td>
          	<td>sex:<%= @cat.sex%></td>
        </tr>
    </tbody>
    <tfoot>
      <tr>
          <th colspan="5"><%= @cat.description %></th>
      </tr>
    </tfoot>
</table>
